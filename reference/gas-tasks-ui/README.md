# Google AI Studio — UI reference (read-only)

Source export from Google AI Studio for visual alignment. **Do not run or import into `frontend/` directly.**

- **Tasks layout:** `extracted/src/components/ProjectsView.tsx`
- **Full app shell:** `extracted/src/App.tsx`, `Sidebar.tsx`, other `*View.tsx` files

TeamBrain ports look + copy from here; APIs and RBAC stay in `frontend/app/[orgSlug]/tasks/`.

To refresh: re-export from Google AI Studio into `extracted/` (do not commit `.zip` archives).
